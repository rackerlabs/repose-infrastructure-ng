source 'https://rubygems.org'

# the two gems you need to do stuff with this project
group :development do
  gem 'hiera-eyaml'
  gem 'librarian-puppet'
  # puppet gem is explicitly needed for the librarian-puppet to use git sources
  gem 'puppet'
end