#Local host only checks, all other

# using dns resolution instead
define host {
    use generic-host
    host_name performance-slave-02
    alias performance-slave-02.openrepose.org
}

# have to do everything via NRPE
