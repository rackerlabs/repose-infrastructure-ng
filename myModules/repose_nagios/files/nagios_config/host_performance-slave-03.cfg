#Local host only checks, all other

# using dns resolution instead
define host {
    use generic-host
    host_name performance-slave-03
    alias performance-slave-03.openrepose.org
}

# have to do everything via NRPE
